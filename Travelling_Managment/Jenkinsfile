pipeline {
    agent any
    stages {
        stage('Pull Images') {
            steps {
                sh 'docker --version'
                sh 'docker-compose down'
            }
        }
    }
}
