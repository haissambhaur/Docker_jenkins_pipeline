pipeline {
    agent any
    stages {
        stage('Pull Images') {
            steps {
                sh 'docker ps'
                sh 'docker pull node:latest'
                sh 'docker-compose --version'
            }
        }
    }
}
