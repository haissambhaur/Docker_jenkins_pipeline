pipeline {
    agent {
        docker {
            image 'docker:dind'
            args '--privileged'
        }
    }
    stages {
        stage('Setup Docker') {
            steps {
                sh 'dockerd & sleep 5' // Start Docker daemon
                sh 'docker --version'
                sh 'docker ps'
            }
        }
    }
}
