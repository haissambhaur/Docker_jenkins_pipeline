pipeline {
    agent any
    stages {
        stage('Pull Images') {
            steps {
                sh 'sudo docker ps'
                sh 'sudo docker-compose down'
                sh 'sudo docker-compose build'
                sh 'sudo docker-compose up'
            }
        }
    }
}
