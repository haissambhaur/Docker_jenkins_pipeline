pipeline {
    agent any
    stages {
        stage('Pull Images') {
            steps {
                sh 'sudo docker ps'
                sh 'sudo docker-compose down'
            }
        }
    }
}
