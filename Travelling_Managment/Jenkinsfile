pipeline {
    agent {
        docker {
            image 'docker:24.0.5-cli'
            args '-v /var/run/docker.sock:/var/run/docker.sock'
        }
    }
    stages {
        stage('Pull Images') {
            steps {
                sh 'docker --version'
            }
        }
    }
}
